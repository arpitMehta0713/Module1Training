package stringExamples;

public class VowelSubstring {
	/*
	 * public static void main(String args[]) { VowelSubstring project = new
	 * VowelSubstring(); project.run(""); }
	 * 
	 * public void run(String word) { String subString = ""; for (int i = 0; i <
	 * word.length(); i++) { /*if (word.charAt(i) == 'a' || word.charAt(i) == 'e' ||
	 * word.charAt(i) == 'i' || word.charAt(i) == 'o' || word.charAt(i) == 'u' ||
	 * word.charAt(i) == 'A' || word.charAt(i) == 'E' || word.charAt(i) == 'I' ||
	 * word.charAt(i) == 'O' || word.charAt(i) == 'U') {
	 * 
	 * }
	 */
}
